"""Destination: gav scraper/scraper.py, morphed - the pure fetch/extract core plus
a CLI for local inspection. Morphs in the pull: seed URLs come from the repo-root
url-list.csv via config (no tier map), and the metadata sidecar carries `section`
alongside source_url and title (cards.py depends on it)."""
