"""Destination: camp backend/config.py - runtime settings. Reshaped in the port:
env-var wiring set by the stack (gav convention), KB id required with no default,
hardcoded account values dropped."""
