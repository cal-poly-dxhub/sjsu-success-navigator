# Chat Lambda deps layer (built by the stack when the lambda section lands).
# Bare handler: no fastapi, no mangum. pydantic stays - the camelCase wire
# contract lives in its aliases (docs/build-plan.md). boto3 comes from the runtime.
pydantic>=2,<3
