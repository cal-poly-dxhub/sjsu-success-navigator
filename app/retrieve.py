"""Destination: camp backend/services/retrieve.py - the Bedrock Knowledge Base
Retrieve wrapper (top-k, min-score filter, metadata extraction incl. section).
Moves in as a file; client hoisted to module scope as part of the port."""
