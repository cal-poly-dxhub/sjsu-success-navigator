"""Destination: camp backend/services/orchestrator.py - the Bedrock Converse
tool-use agent loop. Moves in as a file (Keep list: no logic changes) with imports
flattened to this directory; boto3 clients hoisted to module scope with retry
config as part of the port."""
