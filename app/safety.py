"""Destination: camp backend/services/safety.py - the deterministic pre-model
safety intercept (hardcoded phrase gate, fixed crisis contacts, output scanner).
Moves in unchanged and runs FIRST, before the guardrail and the loop
(docs/synthesis.md ordering decision)."""
