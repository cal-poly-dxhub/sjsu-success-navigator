# No generative-ai-cdk-constructs and no aws-bedrock-alpha: this stack is L1 Cfn* throughout.
aws-cdk-lib==2.260.0
constructs>=10.5.0,<11.0.0
# The CDK app reads the repo-root config.yaml at synth time.
PyYAML>=6.0,<7.0
